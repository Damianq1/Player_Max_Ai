# ULTIMATE FIX Gradle 9.4 + Kotlin BuildFlowService CRASH
# BEZ USUWANIA ŻADNYCH ZALEŻNOŚCI!

# Wyłącz Kotlin statistics (źródło crash)
kotlin.build.reporting.build-flow-service.enabled=false
kotlin.build.reporting.telemetry.enabled=false

# Gradle JVM settings
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m -Dfile.encoding=UTF-8
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Kotlin settings (zachowaj wszystko!)
kotlin.code.style=official
kotlin.jvm.target.validation.mode=warning
