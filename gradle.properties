# ULTIMATE FIX: Kotlin BuildFlowService + Gradle 9.4 HasConvention CRASH
# 100% FUNKCJI ZACHOWANE!

# BEZPOŚREDNIE WYŁĄCZENIE BuildFlowService (źródło crash)
kotlin.build.reporting.build-flow-service.enabled=false
kotlin.build.reporting.telemetry.enabled=false
kotlin.build.reporting.api.enabled=false

# Gradle performance (dla GitHub Actions)
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=512m
org.gradle.parallel=true
org.gradle.caching=true
